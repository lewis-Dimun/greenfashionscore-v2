// Converted to ESM-compatible TS is preferred, but keep .mjs with export default
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true
};

export default nextConfig;


