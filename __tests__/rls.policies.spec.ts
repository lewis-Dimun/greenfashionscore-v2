// RLS tests scaffold – skipped until test DB is configured
describe.skip("RLS policies (Supabase)", () => {
  it("user can only read own submissions/scores; admin can read all", async () => {
    // TODO: seed two users, one admin, create submissions and scores
    // Assert RLS behavior via PostgREST or direct connection with JWTs
  });
});


